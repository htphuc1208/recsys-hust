version: '3.8'

services:
  recsys-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=info
    volumes:
      - ./artifacts:/app/artifacts:ro
    restart: unless-stopped
