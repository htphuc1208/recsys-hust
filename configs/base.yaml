seed: 42
device: "cuda" # "cuda", "cpu", or "mps"
num_workers: 4

logging:
  level: "INFO"
  use_wandb: false
  project_name: "recsys-hust"
  experiment_name: "default_run"

paths:
  data_dir: "data"
  artifacts_dir: "artifacts"
  reports_dir: "reports"
